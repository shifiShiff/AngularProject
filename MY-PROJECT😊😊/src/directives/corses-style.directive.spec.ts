import { CorsesStyleDirective } from './corses-style.directive';

describe('CorsesStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CorsesStyleDirective();
    expect(directive).toBeTruthy();
  });
});
